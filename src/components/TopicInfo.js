import React from 'react';
// import fakeData from '../fakeData';

const TopicInfo = ({ match }) => {
  return (
    <div>
      <h2>test</h2>
      <h3>{console.log(match)}</h3>
    </div>
  );
};

export default TopicInfo;
